# The command to run to start your server
run:
  command: npm start
# Specifies the build tool and build commands to run before deploying.
build:
  buildTool: npm
  buildCommands:
    - npm run build
# The version of the App Hosting config.
version: 2
